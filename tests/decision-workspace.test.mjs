import assert from"node:assert/strict";import{DecisionAccessError,DecisionConflictError,DecisionService,DecisionValidationError}from"../lib/workspace/decisions.ts";const now="2026-07-24T00:00:00Z",finding={id:"i",publicId:"finding-1",title:"Registry mismatch",severity:"high",confidence:"high"};const actor=(role="analyst",org="org-a")=>({userId:"u",organizationId:org,role,name:"A",email:"a@x.com"});class Store{decision=null;async get(a){return a.organizationId==="org-a"?{decision:this.decision&&this.dto(this.decision),availableFindings:[finding]}:null}dto(d){const{caseId,organizationId,createdBy,updatedBy,...safe}=d;return safe}async create(a,c,i){if(this.decision)return null;return this.decision={id:"d",publicId:"decision-1",caseId:c,organizationId:a.organizationId,...i,findings:[finding],version:1,createdBy:a.userId,updatedBy:a.userId,createdAt:now,updatedAt:now}}async update(a,c,i){if(!this.decision)return null;if(i.version!==this.decision.version)return"conflict";return Object.assign(this.decision,i,{version:i.version+1})}}const store=new Store(),service=new DecisionService(store),valid={outcome:"review",rationale:"Manual review is required for the ownership mismatch.",risk:"high",confidence:"high",recommendedActions:["Verify ownership"],conditions:[],findingIds:["finding-1"]};const d=await service.create(actor(),"case-1",valid);assert.equal(d.version,1);assert.equal("organizationId"in d,false);await assert.rejects(()=>service.create(actor("viewer"),"case-2",valid),DecisionAccessError);await assert.rejects(()=>new DecisionService(new Store()).create(actor(),"case-1",{...valid,findingIds:[]}),DecisionValidationError);assert.equal((await new DecisionService(new Store()).create(actor(),"case-1",{...valid,outcome:"no_decision",findingIds:[]})).outcome,"no_decision");const updated=await service.update(actor(),"case-1",{...valid,outcome:"pass",version:1});assert.equal(updated.version,2);await assert.rejects(()=>service.update(actor(),"case-1",{...valid,version:1}),DecisionConflictError);await assert.rejects(()=>service.get(actor("analyst","org-b"),"case-1"));console.log("decision workspace behavioral tests passed");
