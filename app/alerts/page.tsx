/* eslint-disable react-hooks/set-state-in-effect */
"use client";
import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import ShadowScoreLayout from "../../components/ShadowScoreLayout";
import { getCurrentSession } from "../../lib/auth";
import { listBrowserAlerts, updateBrowserAlertStatus } from "../../lib/websiteIntelligence/browserMonitoring";
import type { WebsiteAlert, WebsiteAlertStatus } from "../../lib/websiteIntelligence/alerts";
const statuses: WebsiteAlertStatus[] = ["New", "Reviewing", "Resolved", "Dismissed"];
export default function AlertsPage() { const [tenant, setTenant] = useState(""); const [alerts, setAlerts] = useState<WebsiteAlert[]>([]); const [severity, setSeverity] = useState(""); const [category, setCategory] = useState(""); const [status, setStatus] = useState(""); const [domain, setDomain] = useState(""); useEffect(() => { const session = getCurrentSession(); if (session) { setTenant(session.userId); setAlerts(listBrowserAlerts(session.userId)); } }, []); const filtered = useMemo(() => alerts.filter((alert) => (!severity || alert.severity === severity) && (!category || alert.category === category) && (!status || alert.status === status) && (!domain || alert.domain.includes(domain.toLowerCase()))), [alerts,severity,category,status,domain]); function update(id: string, next: WebsiteAlertStatus) { setAlerts(updateBrowserAlertStatus(tenant,id,next)); }
return <ShadowScoreLayout><section className="mx-auto max-w-7xl px-6 py-16"><div className="flex justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.3em] text-red-300">Website Intelligence</p><h1 className="mt-4 text-4xl font-black">Risk Alerts</h1><p className="mt-3 text-zinc-400">Review meaningful website regressions and record their status.</p></div><Link href="/watchlist" className="font-bold text-red-300">Watchlist</Link></div><div className="mt-8 grid gap-3 rounded-3xl border border-white/10 p-5 sm:grid-cols-4"><input aria-label="Filter by domain" placeholder="Domain" value={domain} onChange={e=>setDomain(e.target.value)} className="rounded-xl border border-white/15 bg-black p-3"/><select aria-label="Filter by severity" value={severity} onChange={e=>setSeverity(e.target.value)} className="rounded-xl border border-white/15 bg-black p-3"><option value="">All severities</option>{["Critical","High","Medium","Low"].map(x=><option key={x}>{x}</option>)}</select><select aria-label="Filter by category" value={category} onChange={e=>setCategory(e.target.value)} className="rounded-xl border border-white/15 bg-black p-3"><option value="">All categories</option>{["SSL/TLS","DNS","HTTP","WHOIS","Security Headers","Reputation"].map(x=><option key={x}>{x}</option>)}</select><select aria-label="Filter by status" value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border border-white/15 bg-black p-3"><option value="">All statuses</option>{statuses.map(x=><option key={x}>{x}</option>)}</select></div><div className="mt-6 space-y-4">{filtered.map(alert=><article key={alert.id} className="rounded-3xl border border-white/10 bg-white/[.035] p-6"><div className="flex flex-wrap justify-between gap-4"><div><span className="text-xs font-black uppercase text-red-300">{alert.severity} · {alert.category}</span><h2 className="mt-2 text-xl font-black">{alert.domain}: {alert.field}</h2><p className="mt-2 text-sm text-zinc-300">{alert.previousValue ?? "Not recorded"} → {alert.currentValue ?? "Not recorded"}</p></div><select aria-label={`Status for ${alert.field}`} value={alert.status} onChange={e=>update(alert.id,e.target.value as WebsiteAlertStatus)} className="h-12 rounded-xl border border-white/15 bg-black p-3">{statuses.map(x=><option key={x}>{x}</option>)}</select></div><p className="mt-4 text-sm text-zinc-400">{alert.explanation}</p><p className="mt-2 text-sm"><strong>Recommended action:</strong> {alert.recommendedAction}</p><div className="mt-4 flex flex-wrap gap-4 text-xs text-zinc-500"><span>{new Date(alert.detectedAt).toLocaleString()}</span><span>{alert.evidenceSource}</span><Link href={`/report?scanId=${encodeURIComponent(alert.previousScanId)}`} className="text-red-300">Previous scan</Link><Link href={`/report?scanId=${encodeURIComponent(alert.currentScanId)}`} className="text-red-300">Current scan</Link></div></article>)}{tenant && filtered.length===0&&<p className="rounded-2xl border border-white/10 p-6 text-zinc-400">No alerts match these filters.</p>}{!tenant&&<p className="rounded-2xl border border-white/10 p-6">Sign in to review alerts.</p>}</div></section></ShadowScoreLayout> }
