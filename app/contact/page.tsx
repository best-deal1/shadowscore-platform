"use client";
import ShadowScoreLayout from "../../components/ShadowScoreLayout";
import { useLocale } from "../../components/LocaleProvider";
import { getUserPageCopy } from "../../lib/i18n";
import { CONTACT_EMAIL, SUPPORT_EMAIL, buildWhatsAppUrl } from "../../lib/config";
export default function ContactPage() { const { locale } = useLocale(); const page = getUserPageCopy(locale).contact; const whatsappUrl = buildWhatsAppUrl(page.description); return <ShadowScoreLayout><section className="mx-auto max-w-4xl px-6 py-20"><div className="text-sm uppercase tracking-[0.28em] text-red-300">{page.eyebrow}</div><h1 className="mt-4 text-5xl font-black">{page.title}</h1><p className="mt-6 text-lg leading-8 text-zinc-400">{page.description}</p><div className="mt-10 rounded-3xl border border-white/10 bg-white/[0.03] p-8"><div className="text-xl font-bold">{page.preferred}</div><p className="mt-4 leading-7 text-zinc-400">{page.instructions}</p><a href={whatsappUrl} target="_blank" rel="noreferrer" className="mt-6 inline-flex rounded-xl bg-red-600 px-7 py-4 font-bold text-white transition hover:bg-red-500">{page.whatsapp}</a></div><div className="mt-8 grid gap-4 md:grid-cols-2"><a href={`mailto:${CONTACT_EMAIL}`} className="rounded-2xl border border-white/10 bg-white/[0.03] p-5 text-zinc-300">{page.general}: {CONTACT_EMAIL}</a><a href={`mailto:${SUPPORT_EMAIL}`} className="rounded-2xl border border-white/10 bg-white/[0.03] p-5 text-zinc-300">{page.support}: {SUPPORT_EMAIL}</a></div></section></ShadowScoreLayout>; }
